const width = 800;
const height = 520;
export const canvas = document.querySelector(".pong__body");
export const c = canvas.getContext("2d");

// Install playground sizes
canvas.setAttribute("width", width);
canvas.setAttribute("height", height);
